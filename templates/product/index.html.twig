{% extends 'base.html.twig' %}

{% block title %}
	produit {{product.name}}
{% endblock %}

{% block body %}
	<div class="row my-5">
		<div class="col-md-6">
			<img src="uploads/{{asset(product.illustration)}}" class="img-fluid" alt="{{product.name}}">
		</div>
		<div class="col-md-6">
			<span>
				<a href="{{path('app_category', {'slug':product.category.slug})}}">{{product.category.name}}</a>
				>
				{{product.name}}</span>
			<h2 class="mb-3">{{product.name}}</h2>
			<span class="d-block my-3">{{ product.priceWT|format_currency('EUR') }}
			<button class="btn btn-primary mt-3 w-100">Ajouter au panier</a>
		</div>
	</div>
{% endblock %}
